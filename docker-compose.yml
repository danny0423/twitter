services:
  app:
    build: .
    ports:
      - "3000:3000"
    volumes:
      - .:/app
      - /app/src/generated/prisma 
    environment:
      - NODE_ENV=development
      - WATCHPACK_POLLING=true
    command: npm run dev:webpack
